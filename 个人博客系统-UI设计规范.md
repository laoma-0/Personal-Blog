# 个人博客系统 UI 设计规范

| 项目名称 | 个人博客系统（Personal Blog） |
| --- | --- |
| 文档版本 | V2.0 |
| 编写日期 | 2026-08-04 |
| 配套文档 | 《个人博客系统 详细设计说明书》 |
| 适用范围 | 前台展示端 + 后台管理端（Vue3 + Element Plus） |
| 设计风格 | 低饱和莫兰迪 / 水彩渐变色调，极缓慢微动效，大面积留白 |

---

## 设计风格总述

本项目采用**低饱和莫兰迪 + 水彩渐变**的柔和视觉基调，追求安静、克制、有呼吸感的阅读氛围：

- **低饱和莫兰迪色调**：所有颜色降低饱和度、提高灰度，色彩之间过渡柔和不刺眼，营造温润、文艺、耐看的气质。
- **水彩渐变**：主视觉（页头、封面遮罩、按钮、卡片高光）采用相邻色相的浅淡线性/径向渐变，模拟水彩晕染，避免纯色块的生硬。
- **大面积留白**：以留白划分层级，间距系统整体放大，内容区收窄，让文字与图片"透气"。
- **极缓慢微动效**：动效时长显著拉长（0.6s ~ 1.2s），缓动平滑，如呼吸般轻柔，服务于氛围而非炫技。

> 一句话概括：**安静、柔和、有呼吸感的水彩系文艺博客。**

---

## 目录

1. [设计原则](#1-设计原则)
2. [色彩规范](#2-色彩规范)
3. [字体规范](#3-字体规范)
4. [间距与栅格](#4-间距与栅格)
5. [圆角、阴影与边框](#5-圆角阴影与边框)
6. [图标与图片](#6-图标与图片)
7. [组件规范](#7-组件规范)
8. [页面布局规范](#8-页面布局规范)
9. [暗黑模式](#9-暗黑模式)
10. [响应式设计](#10-响应式设计)
11. [动效规范](#11-动效规范)
12. [设计令牌（Design Tokens）](#12-设计令牌design-tokens)

---

## 1. 设计原则

| 原则 | 说明 |
| --- | --- |
| 柔和克制 | 低饱和莫兰迪色调，色彩温润不刺眼，以内容为核心 |
| 呼吸留白 | 大面积留白划分层级，间距放大，让内容"透气" |
| 水彩晕染 | 主视觉以浅淡渐变替代纯色块，模拟水彩过渡 |
| 缓慢温柔 | 微动效极慢而平滑，如呼吸般轻柔，营造安静氛围 |
| 可读性优先 | 尽管低对比，正文仍需满足长文阅读的清晰度 |
| 一致性 | 前台与后台共用同一套色彩、字体、间距令牌 |
| 无障碍 | 正文文字与背景对比度尽量接近 WCAG AA（≥ 4.5:1），低饱和不等于低对比 |

> **注意**：低饱和风格容易牺牲对比度。正文文字须选用足够深的莫兰迪灰（而非浅灰），确保长文可读。

---

## 2. 色彩规范

> 全部采用**低饱和莫兰迪色系**：降低饱和度、混入灰调，色彩柔和不刺眼。主色与功能色饱和度普遍控制在 20%~40%。

### 2.1 主色（Primary）

以柔和的**莫兰迪雾蓝（灰蓝）**为主色，温润文艺，不喧宾夺主。

| 名称 | 色值 | 用途 |
| --- | --- | --- |
| Primary | `#8AA6B8` | 主按钮、链接、选中态、强调（雾蓝灰） |
| Primary Hover | `#7A97AA` | 悬停加深 |
| Primary Active | `#6A889C` | 按下 |
| Primary Light | `#E8EEF2` | 浅色背景、标签底色 |

### 2.2 辅助莫兰迪色（点缀）

用于插画、图标、区块点缀，与主色协调。

| 名称 | 色值 | 说明 |
| --- | --- | --- |
| 莫兰迪藕粉 | `#C9AEB0` | 柔粉 |
| 莫兰迪豆绿 | `#A9B8A0` | 灰绿 |
| 莫兰迪杏橘 | `#D2B49C` | 暖杏 |
| 莫兰迪雾紫 | `#B0A6C0` | 灰紫 |
| 莫兰迪灰青 | `#9DB8B6` | 灰青 |

### 2.3 水彩渐变（主视觉）

主视觉（页头、封面遮罩、按钮高光、卡片背景晕染）使用相邻莫兰迪色相的浅淡渐变，模拟水彩晕染。角度以柔和的对角为主（约 135°）。

| 名称 | 渐变值 | 用途 |
| --- | --- | --- |
| Gradient Header | `linear-gradient(135deg, #E8EEF2 0%, #F3ECEF 50%, #EDF0EA 100%)` | 页头背景、大 Banner |
| Gradient Primary | `linear-gradient(135deg, #9DB8B6 0%, #8AA6B8 100%)` | 主按钮、强调块 |
| Gradient Soft | `linear-gradient(135deg, #F5EFEF 0%, #EEF2F4 100%)` | 卡片悬浮高光、空状态背景 |
| Gradient Watercolor | `radial-gradient(circle at 30% 20%, #E8EEF2 0%, transparent 60%), radial-gradient(circle at 80% 70%, #F3ECEF 0%, transparent 55%)` | 页面大面积氛围底纹（低透明叠加） |

> 渐变务必浅淡、透明度低（可叠加 `opacity: 0.5~0.7`），保证其上文字可读。

### 2.4 功能色（语义色 · 低饱和版）

| 名称 | 色值 | 用途 |
| --- | --- | --- |
| Success | `#8FB89A` | 成功提示、发布状态（灰绿） |
| Warning | `#D8B98A` | 警告、草稿状态（灰橙） |
| Danger | `#C89592` | 删除、错误（灰玫瑰红） |
| Info | `#A6ADB4` | 一般信息、次要文字（灰蓝） |

### 2.5 中性色（文字与背景）

中性色统一带极淡暖灰调，避免纯黑纯白的生硬。

| 名称 | 色值 | 用途 |
| --- | --- | --- |
| Text Primary | `#3A3B3C` | 标题、正文主文字（深莫兰迪灰，保证可读） |
| Text Regular | `#5C5E60` | 普通文字 |
| Text Secondary | `#8C8E90` | 辅助文字、时间、摘要 |
| Text Placeholder | `#BEC0C2` | 占位符 |
| Border | `#E6E3E0` | 边框、分割线（暖灰） |
| Border Light | `#F1EFEC` | 浅分割线 |
| Background | `#FDFCFA` | 卡片、内容区背景（微暖白） |
| Background Page | `#F6F4F1` | 页面整体背景（米灰白） |

### 2.6 标签色板（文章标签用）

标签统一使用低饱和莫兰迪色，`tag.color` 字段存储对应色值，展示时用其浅色版做底：

| 色值 | 预览用途 |
| --- | --- |
| `#8AA6B8` | 雾蓝 |
| `#A9B8A0` | 豆绿 |
| `#C9AEB0` | 藕粉 |
| `#B0A6C0` | 雾紫 |
| `#D2B49C` | 杏橘 |
| `#9DB8B6` | 灰青 |

### 2.7 使用约束

- **以留白和渐变为主视觉，纯色块尽量少用**；需要强调时用浅淡渐变而非饱和纯色。
- 主色不滥用，一个页面主行动点（CTA）通常只用一个主色/渐变按钮。
- 大面积背景使用微暖白/米灰白中性色，可叠加极低透明度的水彩底纹。
- 功能色仅用于对应语义，保持低饱和统一调性。
- 严禁使用高饱和荧光色，破坏莫兰迪整体氛围。

---

## 3. 字体规范

### 3.1 字体族

```
中文：'PingFang SC', 'Microsoft YaHei', 'Hiragino Sans GB'
英文/数字：'Inter', 'Helvetica Neue', Arial
代码：'JetBrains Mono', 'Fira Code', Consolas, monospace
```

统一 font-family 声明：

```css
font-family: 'Inter', 'PingFang SC', 'Microsoft YaHei', -apple-system,
             BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

### 3.2 字号与字重层级

字重整体偏轻（多用 300/400），营造柔和文艺气质；行高放大以配合留白。

| 层级 | 字号 | 字重 | 行高 | 用途 |
| --- | --- | --- | --- | --- |
| H1 | 30px | 500 | 1.5 | 页面主标题、文章标题（细体大字更文艺） |
| H2 | 24px | 500 | 1.5 | 区块标题 |
| H3 | 19px | 500 | 1.6 | 小节标题 |
| Body Large | 17px | 400 | 1.9 | 文章正文（大行高，阅读舒适） |
| Body | 15px | 400 | 1.75 | 常规文字、列表 |
| Caption | 13px | 300 | 1.6 | 时间、辅助信息 |

> 字距（letter-spacing）：中文正文 `0.02em`，大标题 `0.01em`，让文字更松弛透气。

### 3.3 正文排版建议

- 文章正文行高 `1.9`，段落间距 `24px`，最大宽度约 `680px`——收窄行宽 + 大行距，强化留白与阅读专注。
- 代码块使用等宽字体，浅米灰底（`#F1EFEC`），字号 `14px`，行高 `1.7`，圆角柔和。
- 链接默认主色（雾蓝灰），悬停缓慢渐变加深并浮现淡淡下划线，避免突兀。
- 标题与正文之间留足空白，段落不拥挤。

---

## 4. 间距与栅格

### 4.1 间距系统（8px 基准 · 大留白）

沿用 8px 基准，但**整体放大档位**，以实现大面积留白的呼吸感。组件内边距、区块间距均取较大值。

| 令牌 | 值 | 典型用途 |
| --- | --- | --- |
| space-xs | 4px | 图标与文字间距 |
| space-sm | 8px | 小间隙 |
| space-md | 16px | 标签内边距、紧凑元素间距 |
| space-lg | 24px | 卡片内边距、元素间距 |
| space-xl | 40px | 区块间距 |
| space-2xl | 64px | 模块间距 |
| space-3xl | 96px | 页面区段间距（大留白） |

> 原则：宁可留白多一点。首页各区段（Banner、文章列表、页脚）之间用 `space-3xl (96px)` 分隔。

### 4.2 栅格布局

- 基于 Element Plus 24 栅格（`el-row` / `el-col`）。
- 前台内容区最大宽度收窄至 `1080px` 居中，两侧留白更充足。
- 常用布局：正文 `17` 栏 + 侧边栏 `6` 栏（中间留 `1` 栏空隙），桌面端。
- 栏间距 `gutter = 40`，比常规更宽松。
- 文章详情正文最大宽度 `680px`，居中，强化专注阅读。

---

## 5. 圆角、阴影与边框

### 5.1 圆角

整体加大圆角，柔化边角，呼应水彩柔和调性。

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| radius-sm | 8px | 按钮、输入框、标签 |
| radius-md | 16px | 卡片 |
| radius-lg | 24px | 弹窗、大卡片、封面图 |
| radius-full | 50% | 头像、圆形按钮 |

### 5.2 阴影

摒弃生硬深阴影，使用**大范围、极低透明度、带暖调**的弥散柔光阴影，营造轻盈漂浮感。

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| shadow-sm | `0 4px 16px rgba(120,110,100,0.05)` | 卡片默认（几乎无形，仅微微浮起） |
| shadow-md | `0 8px 32px rgba(120,110,100,0.07)` | 悬浮卡片、下拉 |
| shadow-lg | `0 16px 48px rgba(120,110,100,0.10)` | 弹窗 |

> 阴影颜色带暖灰调（非纯黑），与米白背景融合，避免脏灰感。

### 5.3 边框

- 尽量少用边框，优先用留白和极淡阴影分隔。
- 必要时默认边框 `1px solid #E6E3E0`（暖灰）。
- 分割线 `1px solid #F1EFEC`，或用大间距替代分割线。

---

## 6. 图标与图片

### 6.1 图标

- 统一使用 **Element Plus Icons**，风格线性。
- 常用图标尺寸：`16px`（行内）/ `20px`（按钮）/ `24px`（导航）。
- 图标颜色跟随文字色，交互态跟随主色。

### 6.2 图片

| 场景 | 规格建议 |
| --- | --- |
| 文章封面 | 16:9，推荐 800×450，圆角 `16px` |
| 头像 | 正方形，圆形裁切，尺寸 40/80/120 |
| 站点 Logo | SVG 优先，高度 32px |

- 列表封面统一裁切比例，`object-fit: cover` 防变形。
- 封面图可叠加一层极浅的水彩渐变遮罩（`Gradient Soft`，透明度低），统一色调。
- 图片默认略微降低饱和度（`filter: saturate(0.9)`），与莫兰迪基调融合；悬停缓慢恢复。
- 图片懒加载，占位使用米白渐变背景（`Gradient Soft`）而非灰块。

---

## 7. 组件规范

### 7.1 按钮

| 类型 | 样式 | 用途 |
| --- | --- | --- |
| 主按钮 | 水彩渐变填充（`Gradient Primary`）、白字 | 页面主操作（发布、提交） |
| 次按钮 | 微暖白底、暖灰细边框、深灰字 | 取消、次要操作 |
| 文字按钮 | 无边框、主色字 | 轻量操作（编辑、更多） |
| 危险按钮 | 灰玫瑰红（`#C89592`） | 删除 |

- 高度：默认 `40px`，小号 `32px`，大号 `48px`（略高，更从容）。
- 圆角 `radius-sm (8px)`，内边距水平 `24px`（更宽松）。
- 悬停：渐变缓慢加深 / 微微上浮，过渡 `0.6s ease`；禁用态降透明度至 `0.5`。
- 不使用重描边和高饱和填充，保持柔和。

### 7.2 输入框 / 表单

- 高度 `40px`，圆角 `8px`，边框 `#E6E3E0`（暖灰），底色微暖白。
- 聚焦态：边框缓慢渐变为主色，带 `3px` 极浅主色柔光（透明度低），过渡 `0.6s`。
- 校验错误：边框灰玫瑰红 + 下方 `13px` 提示文字。
- 表单标签顶部对齐，标签与控件间距 `12px`，字段之间间距 `24px`，留白充足。

### 7.3 卡片（文章卡片）

```
┌─────────────────────────────────────┐
│                                       │
│   [封面图 16:9, 圆角16px]            │
│                                       │
│   标题（H3, 中等字重, 最多2行省略）  │
│                                       │
│   摘要（Body, 灰, 最多2行省略）      │
│                                       │
│   分类 · 标签 · 时间 · 阅读（Caption）│
└─────────────────────────────────────┘
```

- 内边距 `24px`，圆角 `radius-md (16px)`，阴影 `shadow-sm`（几乎无形）。
- 底色微暖白，可选叠加极浅水彩底纹。
- 悬停：缓慢上浮 `4px` + 阴影渐变为 `shadow-md`，过渡 `0.8s cubic-bezier(0.22,1,0.36,1)`——极缓慢、柔顺。
- 卡片间距（列表）`40px`，充分留白。

### 7.4 标签（Tag）

- 圆角 `8px`，内边距 `4px 12px`，字号 `13px`。
- 背景为标签色的浅色版（约 12% 透明度），文字为标签色的加深版，保持低饱和。
- 悬停底色缓慢加深，过渡 `0.6s`。

### 7.5 分页

- 使用 Element Plus `el-pagination`，居中。
- 当前页用主色浅底 + 主色字（非高饱和填充），切换过渡缓慢。

### 7.6 评论组件

- 顶级评论与回复通过左侧缩进（`32px`）体现层级，间距宽松。
- 头像 `44px` 圆形 + 昵称（中等字重）+ 时间（Caption 灰）+ 内容。
- 评论块之间留白 `32px`，「回复」为文字按钮。

### 7.7 消息提示

| 类型 | 用途 |
| --- | --- |
| Message（顶部轻提示） | 操作成功/失败反馈，缓慢淡入淡出 |
| MessageBox（确认框） | 删除等二次确认 |
| Notification（右上通知） | 系统通知 |

- 提示配色使用低饱和功能色，出现/消失动效缓慢（约 `0.6s` 淡入）。

### 7.8 空状态与加载

- 空状态：居中线性插画/图标（莫兰迪色）+ 灰色说明文字（如「这里还很安静～」），背景可叠加极浅水彩渐变。
- 加载：Skeleton 骨架屏（列表），骨架呼吸微光动画缓慢（约 `1.5s` 一次循环）；或 Loading 柔光遮罩（提交）。

---

## 8. 页面布局规范

### 8.1 前台整体布局

```
┌─────────────────────────────────────────────┐
│  Header（Logo | 导航 | 搜索 | 主题切换）      │ 高 64px，固定顶部
├─────────────────────────────────────────────┤
│                                               │
│   ┌───────────────────┐  ┌───────────────┐   │
│   │  主内容区 (18栏)  │  │ 侧边栏 (6栏)  │   │
│   │  文章列表/详情    │  │ 作者卡/标签云 │   │
│   │                   │  │ 最新评论      │   │
│   └───────────────────┘  └───────────────┘   │
│                                               │
├─────────────────────────────────────────────┤
│  Footer（版权 | 备案号 | 友链）               │
└─────────────────────────────────────────────┘
```

- Header 高 `72px`，背景为 `Gradient Header` 水彩渐变（或半透明毛玻璃），无重边框，仅底部极淡分割。
- 内容区最大宽度 `1080px` 居中，页面背景 `#F6F4F1`（米灰白），可叠加极浅水彩底纹；内容卡片微暖白。
- 各区段之间用 `space-3xl (96px)` 大留白分隔。

### 8.2 文章详情页

- 顶部：标题（H1，细体大字）+ 元信息（分类、时间、阅读量），上下留白充足。
- 正文：Markdown 渲染，最大宽度 `680px`，行高 `1.9`，段距 `24px`。
- 右侧或悬浮：文章目录（TOC），滚动缓慢高亮当前章节。
- 底部：标签、上/下篇导航、评论区，区块之间大间距。

### 8.3 后台整体布局

```
┌──────────┬──────────────────────────────────┐
│          │  顶栏（面包屑 | 用户头像 | 登出） │ 高 64px
│ 侧边菜单 ├──────────────────────────────────┤
│ (固定)   │                                  │
│ 仪表盘   │        内容区（卡片包裹）        │
│ 文章管理 │                                  │
│ 分类标签 │                                  │
│ 评论留言 │                                  │
│ 图床设置 │                                  │
└──────────┴──────────────────────────────────┘
```

- 侧边菜单宽度 `240px`（可折叠为 `72px`），浅色莫兰迪主题，选中项用主色浅底 + 缓慢过渡。
- 内容区背景 `#F6F4F1`，内部卡片微暖白、圆角 `16px`、内边距 `32px`，留白宽松。
- 表格页：顶部筛选区 + 表格 + 底部分页，各区块间距 `24px`。

---

## 9. 暗黑模式

支持明暗双主题，通过 CSS 变量切换。暗色同样保持**低饱和莫兰迪调性**——不用纯黑，采用带暖调的深灰褐，色彩依旧柔和。

| 令牌 | 亮色 | 暗色 |
| --- | --- | --- |
| 页面背景 | `#F6F4F1` | `#232120` |
| 卡片背景 | `#FDFCFA` | `#2C2A28` |
| 主文字 | `#3A3B3C` | `#DAD6D0` |
| 次文字 | `#8C8E90` | `#9A958E` |
| 边框 | `#E6E3E0` | `#3A3734` |
| 主色 | `#8AA6B8` | `#93AEC0` |
| 水彩渐变 | 浅淡莫兰迪 | 更低透明度的深调莫兰迪渐变 |

- 切换按钮置于 Header，状态存 `localStorage`，跟随用户系统偏好。
- 暗色下水彩渐变透明度进一步降低，代码块、图片适度降亮，保持柔和不刺眼。

---

## 10. 响应式设计

### 10.1 断点

| 断点 | 宽度 | 布局策略 |
| --- | --- | --- |
| xs（手机） | < 768px | 单列，侧边栏移至底部或隐藏，导航折叠为汉堡菜单 |
| sm（平板） | 768 ~ 992px | 内容区全宽，侧边栏可隐藏 |
| md（小屏） | 992 ~ 1200px | 双列，侧边栏变窄 |
| lg（桌面） | ≥ 1200px | 标准双列布局，内容区 1080px |

### 10.2 移动端要点

- 触控目标最小 `44×44px`。
- 文章正文左右留白 `24px`（移动端也保持呼吸感）。
- 大留白在小屏可适度收缩（区段间距降至 `48~64px`），但不拥挤。
- 后台管理在移动端以只读/简化为主，主要操作建议桌面完成。

---

## 11. 动效规范

**核心理念：极缓慢、极轻柔的微动效。** 所有动效都应「慢下来」，如呼吸、如水彩晕开，让界面安静从容，绝不快速弹跳或夸张。

### 11.1 时长与缓动

| 场景 | 时长 | 缓动 |
| --- | --- | --- |
| 悬停 / 颜色 / 边框过渡 | 0.6s | ease |
| 卡片上浮 / 阴影变化 | 0.8s | `cubic-bezier(0.22, 1, 0.36, 1)`（柔顺缓出） |
| 弹窗 / 抽屉出现 | 0.8s | ease-in-out |
| 页面路由切换（淡入淡出） | 0.8s ~ 1.0s | fade |
| 元素进入视口（渐显上移） | 1.0s ~ 1.2s | ease-out |
| 呼吸 / 骨架微光循环 | 1.5s ~ 2.5s | ease-in-out（无限循环） |

> 统一缓动首选 `cubic-bezier(0.22, 1, 0.36, 1)`（慢入极缓出），营造「轻轻放下」的柔和感。

### 11.2 常用微动效

- **渐显上移（内容进场）**：元素进入视口时，`opacity 0→1` + `translateY(16px→0)`，时长 `1.0~1.2s`，配合 IntersectionObserver 逐个错峰触发（stagger `0.1~0.2s`）。
- **呼吸光晕**：Banner / 头像 / 强调元素可加极缓慢的柔光呼吸（`opacity` 或 `box-shadow` 在 `2s` 周期内轻微起伏）。
- **水彩流动**：页头渐变背景可用 `background-position` 缓慢位移（周期 `20s+`），模拟水彩极缓流动，几乎不易察觉。
- **悬停浮起**：卡片/图片悬停缓慢上浮并放大 `1.01~1.02`，`0.8s` 缓出。
- **链接下划线**：悬停时下划线由中间向两侧缓慢展开（`0.6s`）。

### 11.3 约束

- 动效服务于氛围与柔和体验，**宁慢勿快，宁少勿多**；避免快速位移、强烈缩放、旋转、弹跳。
- 单页面同时运行的动效数量克制，避免视觉嘈杂。
- 尊重系统「减少动态效果」偏好：`@media (prefers-reduced-motion: reduce)` 时关闭非必要动画（尤其循环呼吸与视差）。

### 11.4 参考实现片段

```css
/* 统一缓慢过渡变量 */
:root {
  --ease-soft: cubic-bezier(0.22, 1, 0.36, 1);
  --dur-slow: 0.8s;
  --dur-slower: 1.2s;
}

.card {
  transition: transform var(--dur-slow) var(--ease-soft),
              box-shadow var(--dur-slow) var(--ease-soft);
}
.card:hover {
  transform: translateY(-4px) scale(1.01);
  box-shadow: var(--shadow-md);
}

/* 进场渐显上移 */
.fade-up {
  opacity: 0;
  transform: translateY(16px);
  transition: opacity var(--dur-slower) ease-out,
              transform var(--dur-slower) var(--ease-soft);
}
.fade-up.is-visible { opacity: 1; transform: none; }

/* 呼吸光晕 */
@keyframes breathe {
  0%, 100% { opacity: 0.85; }
  50%      { opacity: 1; }
}
.breathe { animation: breathe 2.4s ease-in-out infinite; }

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
  }
}
```

---

## 12. 设计令牌（Design Tokens）

可直接落地为前端 CSS 变量，在 `:root` 中定义，全局复用。

```css
:root {
  /* 主色（莫兰迪雾蓝灰） */
  --color-primary: #8AA6B8;
  --color-primary-hover: #7A97AA;
  --color-primary-active: #6A889C;
  --color-primary-light: #E8EEF2;

  /* 辅助莫兰迪色 */
  --color-morandi-pink: #C9AEB0;
  --color-morandi-green: #A9B8A0;
  --color-morandi-apricot: #D2B49C;
  --color-morandi-purple: #B0A6C0;
  --color-morandi-cyan: #9DB8B6;

  /* 水彩渐变 */
  --gradient-header: linear-gradient(135deg, #E8EEF2 0%, #F3ECEF 50%, #EDF0EA 100%);
  --gradient-primary: linear-gradient(135deg, #9DB8B6 0%, #8AA6B8 100%);
  --gradient-soft: linear-gradient(135deg, #F5EFEF 0%, #EEF2F4 100%);

  /* 功能色（低饱和版） */
  --color-success: #8FB89A;
  --color-warning: #D8B98A;
  --color-danger: #C89592;
  --color-info: #A6ADB4;

  /* 中性色（暖调） */
  --color-text-primary: #3A3B3C;
  --color-text-regular: #5C5E60;
  --color-text-secondary: #8C8E90;
  --color-text-placeholder: #BEC0C2;
  --color-border: #E6E3E0;
  --color-border-light: #F1EFEC;
  --color-bg: #FDFCFA;
  --color-bg-page: #F6F4F1;

  /* 字号（偏大、行高松弛） */
  --font-h1: 30px;
  --font-h2: 24px;
  --font-h3: 19px;
  --font-body-lg: 17px;
  --font-body: 15px;
  --font-caption: 13px;
  --line-height-body: 1.9;

  /* 间距（8px 基准 · 大留白） */
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 40px;
  --space-2xl: 64px;
  --space-3xl: 96px;

  /* 圆角（加大） */
  --radius-sm: 8px;
  --radius-md: 16px;
  --radius-lg: 24px;

  /* 阴影（弥散暖调柔光） */
  --shadow-sm: 0 4px 16px rgba(120,110,100,0.05);
  --shadow-md: 0 8px 32px rgba(120,110,100,0.07);
  --shadow-lg: 0 16px 48px rgba(120,110,100,0.10);

  /* 动效（极缓慢） */
  --ease-soft: cubic-bezier(0.22, 1, 0.36, 1);
  --dur-base: 0.6s;
  --dur-slow: 0.8s;
  --dur-slower: 1.2s;

  /* 布局（收窄内容区、大留白） */
  --layout-max-width: 1080px;
  --content-width: 680px;   /* 文章正文宽度 */
  --header-height: 72px;
  --sidebar-width: 240px;
}
```

> 建议：以上令牌可映射为 Element Plus 的主题变量（如 `--el-color-primary: var(--color-primary)`、`--el-border-radius-base: var(--radius-sm)`），实现组件库与自定义莫兰迪水彩风格的统一。Element Plus 的功能色（success/warning/danger）也建议覆盖为低饱和版，保持整体调性一致。

---

*本 UI 设计规范与《详细设计说明书》配套使用，可随视觉迭代持续更新。*
