# 个人博客系统 详细设计说明书

| 项目名称 | 个人博客系统（Personal Blog） |
| --- | --- |
| 文档版本 | V1.0 |
| 编写日期 | 2026-08-04 |
| 技术栈 | Spring Boot + MyBatis-Plus + MySQL / Vue3 + Element Plus |
| 文档用途 | 作为系统开发依据 |
| 编写人 | （作者本人） |

---

## 目录

1. [引言](#1-引言)
2. [系统总体设计](#2-系统总体设计)
3. [技术选型与开发环境](#3-技术选型与开发环境)
4. [功能模块设计](#4-功能模块设计)
5. [数据库设计](#5-数据库设计)
6. [后端详细设计](#6-后端详细设计)
7. [REST API 接口设计](#7-rest-api-接口设计)
8. [前端详细设计](#8-前端详细设计)
9. [安全设计](#9-安全设计)
10. [部署与运维设计](#10-部署与运维设计)
11. [开发计划与里程碑](#11-开发计划与里程碑)
12. [附录](#12-附录)

---

## 1. 引言

### 1.1 编写目的

本文档为《个人博客系统》的详细设计说明书，面向系统开发者（即本人），描述系统的总体架构、模块划分、数据库结构、接口定义、前后端设计及部署方案，作为编码实现的直接依据。

### 1.2 项目背景

作者为计算机应用技术专业专科毕业、即将升入南宁职业技术大学软件工程技术专业的学生，已掌握 Java / Spring / JavaEE / Vue / MySQL / HTML / JavaScript 等技术。本项目旨在：

- 沉淀个人技术文章、学习笔记与作品；
- 作为求职 / 升本后的作品集与技术实践项目；
- 完整走一遍「前后端分离 + 数据库 + 部署上线」的工程流程。

### 1.3 术语与缩略语

| 术语 | 说明 |
| --- | --- |
| 前台 | 面向所有访客的博客展示端 |
| 后台 | 面向博主本人的内容管理端（需登录） |
| JWT | JSON Web Token，无状态登录凭证 |
| DTO | 数据传输对象（接口出入参） |
| VO | 视图对象（返回给前端的数据） |
| PO / Entity | 持久化对象（与数据库表映射） |
| SPA | 单页应用（Single Page Application） |

### 1.4 系统目标

| 目标 | 说明 |
| --- | --- |
| 功能完整 | 覆盖文章发布、分类标签、评论留言、后台管理、搜索归档、访问统计、图床 |
| 前后端分离 | Vue 前端 + Spring Boot REST 后端，通过 JSON 通信 |
| 可维护 | 分层清晰、命名规范、统一响应体、统一异常处理 |
| 可上线 | 支持 Linux 服务器 + Nginx + MySQL 部署 |

---

## 2. 系统总体设计

### 2.1 总体架构

系统采用**前后端分离**架构，分为三大部分：前台展示端、后台管理端、后端服务。

```
                         ┌─────────────────────────────────────────┐
        访客浏览器  ─────▶│  前台 SPA (Vue3 + Element Plus)          │
                         │  文章列表 / 详情 / 分类 / 标签 / 归档     │
        博主浏览器  ─────▶│  后台 SPA (Vue3 + Element Plus)          │
                         │  仪表盘 / 文章管理 / 评论管理 / 图床      │
                         └───────────────────┬─────────────────────┘
                                             │  HTTP + JSON (REST)
                                             ▼
                         ┌─────────────────────────────────────────┐
                         │            Nginx (反向代理)              │
                         │   /api → 后端 8080  ；/ → 前端静态资源    │
                         └───────────────────┬─────────────────────┘
                                             ▼
                         ┌─────────────────────────────────────────┐
                         │      后端服务 (Spring Boot)              │
                         │  Controller → Service → Mapper           │
                         │  JWT 鉴权 / 全局异常 / 统一响应          │
                         └──────┬───────────────────────┬──────────┘
                                ▼                       ▼
                     ┌────────────────┐      ┌────────────────────┐
                     │  MySQL 数据库  │      │  文件存储（图床）  │
                     │                │      │  本地磁盘 / 对象存储 │
                     └────────────────┘      └────────────────────┘
```

### 2.2 分层设计（后端）

| 层次 | 职责 | 对应包 |
| --- | --- | --- |
| Controller 层 | 接收请求、参数校验、返回统一响应体 | `controller` |
| Service 层 | 业务逻辑、事务控制 | `service` / `service.impl` |
| Mapper 层 | 数据库访问（MyBatis-Plus） | `mapper` |
| Entity 层 | 数据库表映射对象 | `entity` |
| DTO/VO 层 | 接口出入参对象 | `dto` / `vo` |
| Common 层 | 统一响应、异常、工具、拦截器 | `common` / `config` |

### 2.3 模块划分

```
个人博客系统
├── 用户与权限模块      登录、鉴权、个人资料
├── 文章模块            发布、编辑、删除、列表、详情、置顶
├── 分类模块            分类的增删改查、文章归类
├── 标签模块            标签的增删改查、多对多关联
├── 评论模块            文章评论、嵌套回复、审核
├── 留言模块            独立留言板
├── 搜索与归档模块      全文搜索、按时间归档
├── 统计模块            访问量、文章阅读量、仪表盘数据
├── 图床/文件模块       图片上传、访问、删除
└── 系统配置模块        博客名称、公告、友链、关于页
```

---

## 3. 技术选型与开发环境

### 3.1 技术栈

| 分类 | 技术 | 版本建议 | 说明 |
| --- | --- | --- | --- |
| 后端框架 | Spring Boot | 3.2.x | 主框架（JDK 17）；若用 JDK 8 则选 2.7.x |
| ORM | MyBatis-Plus | 3.5.x | 简化 CRUD、分页、条件构造器 |
| 数据库 | MySQL | 8.0 | 关系型数据库 |
| 连接池 | HikariCP | 内置 | 默认连接池 |
| 鉴权 | JWT (jjwt) | 0.11.x | 无状态登录 |
| 缓存(可选) | Redis | 7.x | 缓存热点文章、验证码、访问计数 |
| 文档 | Knife4j / Swagger | 4.x | 接口文档 |
| 前端框架 | Vue | 3.4.x | 组合式 API |
| 构建工具 | Vite | 5.x | 前端构建 |
| UI 组件 | Element Plus | 2.x | 后台管理 UI |
| 路由 | Vue Router | 4.x | 前端路由 |
| 状态管理 | Pinia | 2.x | 全局状态 |
| HTTP | Axios | 1.x | 请求库 |
| Markdown | md-editor-v3 | 4.x | 文章编辑与渲染 |
| 部署 | Nginx + Linux | - | 反向代理与静态托管 |

### 3.2 开发环境

| 项 | 配置 |
| --- | --- |
| JDK | 17（或 8） |
| Maven | 3.8+ |
| Node.js | 18+ |
| IDE | IntelliJ IDEA（后端）+ VS Code（前端） |
| 数据库工具 | Navicat / DataGrip |
| 版本控制 | Git + GitHub / Gitee |

### 3.3 工程目录结构

**后端（blog-backend）**

```
blog-backend
├── src/main/java/com/blog
│   ├── BlogApplication.java
│   ├── controller/         # 控制器
│   ├── service/            # 业务接口
│   │   └── impl/           # 业务实现
│   ├── mapper/             # MyBatis-Plus Mapper
│   ├── entity/             # 数据库实体
│   ├── dto/                # 请求参数对象
│   ├── vo/                 # 返回视图对象
│   ├── common/             # Result、异常、枚举、常量
│   ├── config/             # 配置类（跨域、MyBatis、Swagger）
│   ├── interceptor/        # JWT 拦截器
│   └── util/               # 工具类（JWT、文件、加密）
├── src/main/resources
│   ├── application.yml
│   └── mapper/             # XML（复杂 SQL 时使用）
└── pom.xml
```

**前端（blog-frontend / blog-admin）**

```
blog-frontend            # 前台（也可与后台合并为一个工程用路由区分）
├── src
│   ├── api/              # 接口封装
│   ├── assets/           # 静态资源
│   ├── components/       # 公共组件
│   ├── views/            # 页面
│   ├── router/           # 路由
│   ├── store/            # Pinia
│   ├── utils/            # request.js 等
│   └── App.vue / main.js
├── vite.config.js
└── package.json
```

---

## 4. 功能模块设计

### 4.1 功能清单（含用例）

| 模块 | 功能 | 前台/后台 | 说明 |
| --- | --- | --- | --- |
| 文章 | 浏览文章列表 | 前台 | 分页、按分类/标签过滤 |
| 文章 | 查看文章详情 | 前台 | Markdown 渲染、阅读量+1 |
| 文章 | 发布/编辑/删除文章 | 后台 | 草稿/发布状态、置顶 |
| 分类 | 查看分类及文章 | 前台 | |
| 分类 | 分类增删改 | 后台 | |
| 标签 | 标签云、按标签筛选 | 前台 | |
| 标签 | 标签增删改 | 后台 | |
| 评论 | 发表评论/回复 | 前台 | 支持嵌套、需审核（可配置） |
| 评论 | 审核/删除评论 | 后台 | |
| 留言 | 留言板留言 | 前台 | 独立于文章 |
| 留言 | 留言管理 | 后台 | |
| 搜索 | 关键词搜索文章 | 前台 | 标题+内容 |
| 归档 | 按年月归档文章 | 前台 | 时间轴 |
| 统计 | 访问量/阅读排行 | 后台 | 仪表盘展示 |
| 图床 | 上传/管理图片 | 后台 | 编辑器插图、封面 |
| 用户 | 登录/登出 | 后台 | JWT |
| 配置 | 博客信息/友链/关于 | 后台 | 站点设置 |

### 4.2 关键业务流程

**（1）文章发布流程**

```
博主登录后台 → 进入"写文章" → 填写标题/正文(Markdown)/摘要/封面
  → 选择分类、添加标签 → 选择状态(草稿/发布) → 提交
  → 后端校验 → 保存文章 → 处理标签多对多关系 → 返回结果
  → 前台文章列表可见（若为发布状态）
```

**（2）评论与嵌套回复流程**

```
访客在文章详情页填写昵称/邮箱/内容 → 提交评论
  → 后端校验(内容非空、频率限制) → 保存(状态=待审核 或 直接通过)
  → 若开启审核：后台审核通过后前台才展示
  → 回复评论时携带 parent_id，构建树形结构展示
```

**（3）访问统计流程**

```
用户访问文章详情 → 后端 read_count + 1（可用 Redis 计数后定时落库）
  → 记录访问日志(IP、时间、UA) → 仪表盘按日/文章聚合展示
```

---

## 5. 数据库设计

### 5.1 设计说明

- 数据库名：`blog`
- 字符集：`utf8mb4`，排序规则：`utf8mb4_general_ci`
- 引擎：InnoDB
- 主键：统一使用 `bigint` 自增 `id`
- 公共字段：`create_time`、`update_time`、逻辑删除 `is_deleted`（0 未删 / 1 已删）

### 5.2 E-R 关系概述

```
用户(1) ──── 发布 ────(N) 文章
文章(N) ──── 属于 ────(1) 分类
文章(N) ──── 多对多 ──(N) 标签      （通过 article_tag 关联表）
文章(1) ──── 拥有 ────(N) 评论      （评论自关联实现嵌套回复）
留言 / 访问日志 / 系统配置 为独立表
```

### 5.3 数据表清单

| 表名 | 中文名 | 说明 |
| --- | --- | --- |
| `sys_user` | 用户表 | 博主账号 |
| `article` | 文章表 | 博客文章 |
| `category` | 分类表 | 文章分类 |
| `tag` | 标签表 | 文章标签 |
| `article_tag` | 文章标签关联表 | 多对多 |
| `comment` | 评论表 | 文章评论（自关联嵌套） |
| `message` | 留言表 | 留言板 |
| `visit_log` | 访问日志表 | 访问统计 |
| `file_record` | 文件表 | 图床/上传记录 |
| `sys_config` | 系统配置表 | 站点设置、友链、关于 |

### 5.4 表结构详情

**（1）sys_user 用户表**

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | PK, 自增 | 主键 |
| username | varchar(50) | 唯一, 非空 | 登录账号 |
| password | varchar(100) | 非空 | BCrypt 加密密码 |
| nickname | varchar(50) | | 昵称 |
| avatar | varchar(255) | | 头像 URL |
| email | varchar(100) | | 邮箱 |
| intro | varchar(255) | | 个人简介 |
| last_login_time | datetime | | 上次登录时间 |
| create_time | datetime | | 创建时间 |
| update_time | datetime | | 更新时间 |

**（2）article 文章表**

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | PK, 自增 | 主键 |
| title | varchar(200) | 非空 | 标题 |
| summary | varchar(500) | | 摘要 |
| content | longtext | 非空 | Markdown 正文 |
| content_html | longtext | | 渲染后的 HTML（可选缓存） |
| cover | varchar(255) | | 封面图 URL |
| category_id | bigint | FK | 所属分类 |
| author_id | bigint | FK | 作者（sys_user.id） |
| status | tinyint | 默认 0 | 0 草稿 / 1 已发布 |
| is_top | tinyint | 默认 0 | 是否置顶 |
| read_count | int | 默认 0 | 阅读量 |
| like_count | int | 默认 0 | 点赞量 |
| create_time | datetime | | 创建时间 |
| update_time | datetime | | 更新时间 |
| is_deleted | tinyint | 默认 0 | 逻辑删除 |

**（3）category 分类表**

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | PK, 自增 | 主键 |
| name | varchar(50) | 唯一, 非空 | 分类名 |
| description | varchar(255) | | 描述 |
| sort | int | 默认 0 | 排序 |
| create_time | datetime | | 创建时间 |

**（4）tag 标签表**

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | PK, 自增 | 主键 |
| name | varchar(50) | 唯一, 非空 | 标签名 |
| color | varchar(20) | | 标签颜色（前台展示用） |
| create_time | datetime | | 创建时间 |

**（5）article_tag 文章标签关联表**

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | PK, 自增 | 主键 |
| article_id | bigint | FK, 非空 | 文章 ID |
| tag_id | bigint | FK, 非空 | 标签 ID |

> 建议对 `(article_id, tag_id)` 建唯一索引，避免重复关联。

**（6）comment 评论表**

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | PK, 自增 | 主键 |
| article_id | bigint | FK, 非空 | 所属文章 |
| parent_id | bigint | 默认 0 | 父评论 ID，0 为顶级 |
| nickname | varchar(50) | 非空 | 评论者昵称 |
| email | varchar(100) | | 评论者邮箱 |
| avatar | varchar(255) | | 头像（可用 gravatar） |
| content | varchar(1000) | 非空 | 评论内容 |
| ip | varchar(50) | | 评论 IP |
| status | tinyint | 默认 0 | 0 待审核 / 1 通过 / 2 拒绝 |
| create_time | datetime | | 创建时间 |

**（7）message 留言表**

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | PK, 自增 | 主键 |
| nickname | varchar(50) | 非空 | 留言人 |
| email | varchar(100) | | 邮箱 |
| content | varchar(1000) | 非空 | 留言内容 |
| ip | varchar(50) | | IP |
| status | tinyint | 默认 0 | 审核状态 |
| create_time | datetime | | 创建时间 |

**（8）visit_log 访问日志表**

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | PK, 自增 | 主键 |
| ip | varchar(50) | | 访问 IP |
| url | varchar(255) | | 访问路径 |
| user_agent | varchar(500) | | 浏览器 UA |
| article_id | bigint | | 关联文章（如为文章访问） |
| visit_time | datetime | | 访问时间 |

**（9）file_record 文件表（图床）**

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | PK, 自增 | 主键 |
| original_name | varchar(255) | | 原始文件名 |
| store_name | varchar(255) | | 存储文件名 |
| url | varchar(500) | | 访问 URL |
| size | bigint | | 文件大小(字节) |
| type | varchar(50) | | MIME 类型 |
| create_time | datetime | | 上传时间 |

**（10）sys_config 系统配置表**

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | PK, 自增 | 主键 |
| config_key | varchar(100) | 唯一 | 配置键（如 blog_name） |
| config_value | text | | 配置值 |
| remark | varchar(255) | | 说明 |

> 友链、公告、关于页、备案号等均以键值对存入本表，便于扩展。

### 5.5 建表 SQL 示例（核心表）

```sql
CREATE DATABASE IF NOT EXISTS blog DEFAULT CHARSET utf8mb4 COLLATE utf8mb4_general_ci;
USE blog;

CREATE TABLE article (
  id            BIGINT       PRIMARY KEY AUTO_INCREMENT,
  title         VARCHAR(200) NOT NULL,
  summary       VARCHAR(500),
  content       LONGTEXT     NOT NULL,
  content_html  LONGTEXT,
  cover         VARCHAR(255),
  category_id   BIGINT,
  author_id     BIGINT,
  status        TINYINT      DEFAULT 0,
  is_top        TINYINT      DEFAULT 0,
  read_count    INT          DEFAULT 0,
  like_count    INT          DEFAULT 0,
  create_time   DATETIME     DEFAULT CURRENT_TIMESTAMP,
  update_time   DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  is_deleted    TINYINT      DEFAULT 0,
  INDEX idx_category (category_id),
  INDEX idx_status_time (status, create_time)
) ENGINE=InnoDB;

CREATE TABLE article_tag (
  id          BIGINT PRIMARY KEY AUTO_INCREMENT,
  article_id  BIGINT NOT NULL,
  tag_id      BIGINT NOT NULL,
  UNIQUE KEY uk_article_tag (article_id, tag_id)
) ENGINE=InnoDB;
```

---

## 6. 后端详细设计

### 6.1 统一响应体

```java
public class Result<T> {
    private Integer code;      // 200 成功，其余为错误码
    private String  message;   // 提示信息
    private T       data;      // 业务数据

    public static <T> Result<T> success(T data) { ... }
    public static <T> Result<T> error(String msg) { ... }
}
```

统一返回示例：

```json
{ "code": 200, "message": "success", "data": { } }
```

### 6.2 全局异常处理

使用 `@RestControllerAdvice` + `@ExceptionHandler` 捕获：

- 参数校验异常（`MethodArgumentNotValidException`）
- 业务异常（自定义 `BusinessException`）
- 鉴权异常（`UnauthorizedException`）
- 兜底异常（`Exception`）

统一转换为 `Result` 返回，避免堆栈直接暴露给前端。

### 6.3 分层职责与命名规范

| 类型 | 命名 | 示例 |
| --- | --- | --- |
| Controller | XxxController | `ArticleController` |
| Service | XxxService / XxxServiceImpl | `ArticleService` |
| Mapper | XxxMapper | `ArticleMapper` |
| Entity | Xxx | `Article` |
| 入参 DTO | XxxDTO / XxxQuery | `ArticleDTO` |
| 出参 VO | XxxVO | `ArticleVO` |

### 6.4 核心业务逻辑要点

- **文章保存**：文章表插入后，处理 `article_tag` 关联（先删后插）；`content_html` 可在保存时由后端渲染或前端提供。
- **阅读量**：详情接口自增，可先写 Redis，定时任务批量落库减少数据库压力（无 Redis 时直接 `UPDATE ... SET read_count = read_count + 1`）。
- **评论树**：查询顶级评论（`parent_id=0`），再递归/一次性查子评论在内存中组装成树。
- **逻辑删除**：MyBatis-Plus 配置 `@TableLogic`，查询自动过滤已删数据。
- **分页**：统一使用 MyBatis-Plus 的 `Page<T>` + 分页插件。

---

## 7. REST API 接口设计

### 7.1 接口规范

- 基础路径：`/api`
- 风格：RESTful，资源名词复数
- 请求/响应：`application/json`
- 鉴权：后台接口在 Header 携带 `Authorization: Bearer <token>`
- 分页参数：`pageNum`（默认1）、`pageSize`（默认10）

### 7.2 接口清单

**认证模块**

| 方法 | 路径 | 说明 | 鉴权 |
| --- | --- | --- | --- |
| POST | `/api/auth/login` | 登录，返回 JWT | 否 |
| POST | `/api/auth/logout` | 登出 | 是 |
| GET | `/api/auth/info` | 获取当前登录用户信息 | 是 |

**文章模块**

| 方法 | 路径 | 说明 | 鉴权 |
| --- | --- | --- | --- |
| GET | `/api/articles` | 文章分页列表（支持分类/标签/关键词） | 否 |
| GET | `/api/articles/{id}` | 文章详情（阅读量+1） | 否 |
| GET | `/api/articles/archive` | 归档（按年月分组） | 否 |
| POST | `/api/admin/articles` | 新增文章 | 是 |
| PUT | `/api/admin/articles/{id}` | 编辑文章 | 是 |
| DELETE | `/api/admin/articles/{id}` | 删除文章 | 是 |
| PUT | `/api/admin/articles/{id}/top` | 置顶/取消置顶 | 是 |

**分类 / 标签模块**

| 方法 | 路径 | 说明 | 鉴权 |
| --- | --- | --- | --- |
| GET | `/api/categories` | 分类列表 | 否 |
| GET | `/api/tags` | 标签列表（标签云） | 否 |
| POST | `/api/admin/categories` | 新增分类 | 是 |
| PUT | `/api/admin/categories/{id}` | 编辑分类 | 是 |
| DELETE | `/api/admin/categories/{id}` | 删除分类 | 是 |
| POST | `/api/admin/tags` | 新增标签 | 是 |
| DELETE | `/api/admin/tags/{id}` | 删除标签 | 是 |

**评论 / 留言模块**

| 方法 | 路径 | 说明 | 鉴权 |
| --- | --- | --- | --- |
| GET | `/api/articles/{id}/comments` | 文章评论树 | 否 |
| POST | `/api/comments` | 发表评论/回复 | 否 |
| GET | `/api/messages` | 留言列表 | 否 |
| POST | `/api/messages` | 提交留言 | 否 |
| GET | `/api/admin/comments` | 评论管理列表 | 是 |
| PUT | `/api/admin/comments/{id}/audit` | 审核评论 | 是 |
| DELETE | `/api/admin/comments/{id}` | 删除评论 | 是 |

**文件 / 统计 / 配置模块**

| 方法 | 路径 | 说明 | 鉴权 |
| --- | --- | --- | --- |
| POST | `/api/admin/files/upload` | 图片上传 | 是 |
| DELETE | `/api/admin/files/{id}` | 删除文件 | 是 |
| GET | `/api/admin/statistics/overview` | 仪表盘概览（文章数/评论数/访问量） | 是 |
| GET | `/api/admin/statistics/visit-trend` | 访问趋势（近30天） | 是 |
| GET | `/api/configs` | 获取站点配置 | 否 |
| PUT | `/api/admin/configs` | 更新站点配置 | 是 |

### 7.3 接口示例

**登录接口**

```
POST /api/auth/login
Content-Type: application/json

请求：
{ "username": "admin", "password": "123456" }

响应：
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1Ni
    ...",
    "userInfo": { "id": 1, "username": "admin", "nickname": "博主" }
  }
}
```

**文章列表接口**

```
GET /api/articles?pageNum=1&pageSize=10&categoryId=2&keyword=Spring

响应：
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 25,
    "list": [
      {
        "id": 12,
        "title": "Spring Boot 入门",
        "summary": "……",
        "cover": "http://.../a.png",
        "categoryName": "后端",
        "tags": ["Java", "Spring"],
        "readCount": 128,
        "createTime": "2026-08-01 10:00:00"
      }
    ]
  }
}
```

---

## 8. 前端详细设计

### 8.1 前台页面（面向访客）

| 页面 | 路由 | 说明 |
| --- | --- | --- |
| 首页 | `/` | 置顶文章 + 最新文章列表 + 侧边栏 |
| 文章详情 | `/article/:id` | Markdown 渲染、目录、评论区 |
| 分类页 | `/category/:id` | 该分类下文章 |
| 标签页 | `/tag/:id` | 该标签下文章 |
| 归档页 | `/archive` | 按年月时间轴 |
| 留言板 | `/message` | 留言列表与表单 |
| 关于 | `/about` | 个人介绍 |
| 搜索结果 | `/search?keyword=` | 搜索结果列表 |

### 8.2 后台页面（面向博主，需登录）

| 页面 | 路由 | 说明 |
| --- | --- | --- |
| 登录 | `/admin/login` | 账号密码登录 |
| 仪表盘 | `/admin/dashboard` | 数据概览、访问趋势图 |
| 文章管理 | `/admin/articles` | 列表、搜索、批量操作 |
| 写/编辑文章 | `/admin/article/edit/:id?` | Markdown 编辑器 |
| 分类管理 | `/admin/categories` | 增删改 |
| 标签管理 | `/admin/tags` | 增删改 |
| 评论管理 | `/admin/comments` | 审核、删除 |
| 留言管理 | `/admin/messages` | 审核、删除 |
| 图床管理 | `/admin/files` | 图片列表、上传 |
| 站点设置 | `/admin/settings` | 博客名、友链、关于 |

### 8.3 公共组件

| 组件 | 说明 |
| --- | --- |
| `Header` / `Footer` | 前台头尾 |
| `ArticleCard` | 文章卡片 |
| `SideBar` | 侧边栏（标签云、最新评论） |
| `CommentList` | 评论树组件（递归） |
| `MarkdownView` | Markdown 渲染 |
| `AdminLayout` | 后台整体布局（侧边菜单+顶栏） |

### 8.4 前端关键技术点

- **请求封装**：`utils/request.js` 基于 Axios，统一 baseURL、请求头注入 token、响应拦截处理 code。
- **登录态**：token 存 `localStorage`，Pinia 管理用户信息；路由守卫拦截未登录访问后台。
- **Markdown**：使用 `md-editor-v3` 同时用于后台编辑与前台预览。
- **代码高亮 / 目录**：文章详情页解析标题生成侧边目录（TOC）。
- **响应式**：前台适配移动端（Element Plus 栅格 + 媒体查询）。

---

## 9. 安全设计

| 风险 | 措施 |
| --- | --- |
| 密码泄露 | BCrypt 加盐存储，不可逆 |
| 越权访问后台 | JWT 拦截器校验，后台接口统一鉴权 |
| SQL 注入 | MyBatis-Plus 参数化查询，禁止拼接 SQL |
| XSS | 评论/留言内容前端转义或后端过滤；Markdown 渲染做白名单 |
| CSRF | 前后端分离 + JWT（非 Cookie 会话）天然降低风险 |
| 跨域 | 后端配置 CORS，仅允许指定前端域名 |
| 评论刷屏 | 同 IP 频率限制、可加验证码 |
| 文件上传 | 校验文件类型/大小，重命名存储，限制目录 |
| 敏感配置 | 数据库密码、JWT 密钥用配置文件/环境变量，勿硬编码进代码库 |

---

## 10. 部署与运维设计

### 10.1 部署架构

```
[用户] → [云服务器 Linux]
              ├── Nginx :80/443
              │     ├── /        → 前端打包 dist 静态文件
              │     └── /api     → 反向代理 → Spring Boot :8080
              ├── Spring Boot (jar, systemd 或 nohup 启动)
              └── MySQL :3306
```

### 10.2 部署步骤

1. **后端打包**：`mvn clean package` 生成 `blog-backend.jar`。
2. **前端打包**：`npm run build` 生成 `dist`。
3. **服务器准备**：安装 JDK、MySQL、Nginx。
4. **导入数据库**：执行建表 SQL 与初始化数据。
5. **启动后端**：`java -jar blog-backend.jar --spring.profiles.active=prod`。
6. **配置 Nginx**：静态托管前端 + 反向代理 `/api`。
7. **域名与 HTTPS**：解析域名，使用 Let's Encrypt 申请证书。

### 10.3 Nginx 配置示例

```nginx
server {
    listen 80;
    server_name yourblog.com;

    location / {
        root /var/www/blog/dist;
        try_files $uri $uri/ /index.html;   # 支持前端路由
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 10.4 备份与监控

- MySQL 定时 `mysqldump` 备份；
- 日志：后端 logback 输出到文件，按天滚动；
- 可选接入简单监控（磁盘、内存）。

---

## 11. 开发计划与里程碑

| 阶段 | 内容 | 建议周期 |
| --- | --- | --- |
| 阶段一 | 环境搭建、数据库设计、后端基础框架（统一响应/异常/JWT） | 1 周 |
| 阶段二 | 文章、分类、标签模块（后端+后台管理页） | 1.5 周 |
| 阶段三 | 前台展示端（首页/详情/分类/标签/归档/搜索） | 1.5 周 |
| 阶段四 | 评论、留言模块（前台+后台审核） | 1 周 |
| 阶段五 | 图床、访问统计、仪表盘、站点配置 | 1 周 |
| 阶段六 | 联调、样式优化、部署上线 | 1 周 |

> 建议边做边用 Git 提交，形成清晰的开发记录，可作为升本后的作品展示。

---

## 12. 附录

### 12.1 初始化数据建议

- 内置一个管理员账号（`admin` / BCrypt 加密密码）。
- 预置若干分类（如：后端、前端、数据库、随笔）。
- 预置站点配置键：`blog_name`、`blog_notice`、`about_content`、`friend_links`、`icp`。

### 12.2 后续可扩展方向

- 文章点赞 / 收藏；
- RSS 订阅；
- 邮件通知（评论被回复时通知）；
- 接入 Elasticsearch 做更强全文检索；
- 微信小程序端（复用后端接口，正好用上你的小程序技能）；
- 暗黑模式 / 多主题。

### 12.3 与个人技术栈的对应

| 你已掌握 | 在本项目中的应用 |
| --- | --- |
| Java / Spring / JavaEE | 后端 Spring Boot 服务 |
| MySQL | 数据库设计与存储 |
| Vue / JavaScript / HTML | 前台与后台前端 |
| Linux | 服务器部署 |
| PS | 封面图、Logo 设计 |
| 微信小程序 | 可作为扩展的小程序端 |

---

*本说明书可随开发进展持续迭代更新。*
