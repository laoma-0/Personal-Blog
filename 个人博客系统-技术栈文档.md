# 个人博客系统 · 技术栈文档

> 本文档固化项目的技术选型、版本与本地开发环境，作为开发的正式技术依据。
> 配套文档：《详细设计说明书》《UI 设计规范 V2.0》《数据库脚本 blog.sql》《原型 prototype/》
> 生成日期：2026-08-04

---

## 1. 项目概述

| 项 | 内容 |
| --- | --- |
| 项目名称 | 个人博客系统 |
| 项目类型 | 前后端分离的 Web 应用 |
| 目标 | 技术学习 + 个人作品，串联 Spring / Vue / MySQL 完整工程流程 |
| 架构模式 | 前后端分离，后端提供 REST API，前端负责展示与交互 |
| 部署形态 | 单机部署，Nginx 反向代理统一前后端 |

---

## 2. 总体架构

```
┌──────────────┐     HTTP/JSON      ┌──────────────────┐      JDBC      ┌────────────┐
│  前端 (Vue3)  │  ───────────────▶ │  后端 (Spring Boot) │ ───────────▶ │  MySQL 8   │
│  前台 + 后台   │  ◀─────────────── │   REST API         │ ◀─────────── │  blog 库    │
└──────────────┘     Result 响应体   └──────────────────┘                └────────────┘
        │                                    │
        └──────────── Nginx 反向代理统一域名 ───┘
```

- **前端**：Vue3 单页应用，含前台（访客浏览）与后台（博主管理）两部分，用路由区分。
- **后端**：Spring Boot 提供 RESTful 接口，统一 `Result` 响应体，JWT 鉴权。
- **数据库**：MySQL 8，共 10 张表（见 `blog.sql`）。
- **反向代理**：Nginx 将前端静态资源与后端 `/api` 接口统一在同一域名下。

---

## 3. 后端技术栈

| 技术 | 版本 | 用途 | 备注 |
| --- | --- | --- | --- |
| JDK | **21.0.12 LTS** | 运行环境 | Temurin，LTS 长期支持 |
| Spring Boot | **3.2.x** | 应用框架 | 需 JDK 17+，与 JDK 21 匹配 |
| Spring Web | 随 Boot | REST 接口 | MVC / RestController |
| Spring Security | 随 Boot | 认证授权 | 配合 JWT |
| MyBatis-Plus | **3.5.x** | ORM 框架 | 简化 CRUD、分页、条件构造 |
| MySQL Connector/J | 8.x | 数据库驱动 | 对接 MySQL 8 |
| JWT (jjwt) | 0.12.x | 无状态登录令牌 | 生成 / 校验 Token |
| Hutool | 5.8.x | 工具类库 | 可选，简化开发 |
| Lombok | 最新 | 简化实体代码 | `@Data` 等注解 |
| Maven | **3.9.16** | 构建管理 | 依赖 / 打包 |

**说明（重要）**：

- 本机曾装 JDK 26（非 LTS），已改用 **JDK 21 LTS** 作为本项目运行环境，资料多、稳定、Spring Boot 官方支持。
- Spring Boot 选 **3.2.x** 而非更低的 2.7.x，因为已用 JDK 21；如后续遇到极冷门兼容问题再评估。
- 初版**不引入 Redis**（阅读量统计等优化项可后期再加）。

---

## 4. 前端技术栈

| 技术 | 版本 | 用途 | 备注 |
| --- | --- | --- | --- |
| Node.js | **24.18.0** | 前端运行/构建环境 | 已安装 |
| npm | **11.16.0** | 包管理 | 随 Node |
| Vue | **3.x** | 前端框架 | 组合式 API |
| Vite | **5.x** | 构建工具 | 开发服务器 + 打包 |
| Vue Router | 4.x | 路由 | 前台/后台路由划分 |
| Pinia | 2.x | 状态管理 | 登录态、全局配置 |
| Element Plus | 最新 | UI 组件库 | 适配莫兰迪水彩主题 |
| Axios | 1.x | HTTP 请求 | 统一封装拦截器 |
| md-editor-v3 | 最新 | Markdown 编辑/渲染 | 文章编辑与展示，可选 |

**说明**：

- UI 主题遵循《UI 设计规范 V2.0》：低饱和莫兰迪 / 水彩渐变 / 大留白 / 极缓慢微动效。
- 设计令牌（CSS 变量）见 `prototype/assets/style.css`，接入全局样式并映射到 Element Plus 的 `--el-color-primary` 等变量。

---

## 5. 数据库

| 项 | 内容 |
| --- | --- |
| 数据库 | MySQL **8.0.46** Community |
| 字符集 | utf8mb4 / utf8mb4_general_ci |
| 库名 | `blog` |
| 表数量 | 10 张（用户/分类/标签/文章/文章标签/评论/留言/访问日志/文件/系统配置） |
| 建表脚本 | [blog.sql](file:///e:/Personal%20Blog/blog.sql) |
| 连接账号 | root（开发环境，密码本地配置，不入库/不外传） |

---

## 6. 开发工具与环境

| 工具 | 版本 | 用途 |
| --- | --- | --- |
| Trae | — | 主力 IDE（当前环境） |
| Git | **2.55.0** | 版本管理 |
| MySQL 客户端 | 8.0.46 | 命令行 / 可选 Navicat、DataGrip |
| 浏览器 | 现代浏览器 | 前端调试 |
| 操作系统 | Windows 11 Pro | 本地开发 |

---

## 7. 本地环境就绪清单

| 组件 | 要求版本 | 本机版本 | 状态 |
| --- | --- | --- | --- |
| JDK | 21 LTS | 21.0.12 | ✅ |
| Maven | 3.9+ | 3.9.16 | ✅ |
| Node.js | 18+ | 24.18.0 | ✅ |
| npm | 9+ | 11.16.0 | ✅ |
| MySQL | 8.0+ | 8.0.46 | ✅ |
| Git | 2.x | 2.55.0 | ✅ |

> 全部就绪，可直接开始搭建后端 / 前端骨架。

---

## 8. 工程目录规划

采用**前后端分离、两个平级目录**：

```
e:\Personal Blog\
├── backend\                  # Spring Boot 后端工程
│   ├── src\main\java\...      # 源码（controller/service/mapper/entity...）
│   ├── src\main\resources\    # 配置（application.yml、mapper xml）
│   └── pom.xml
├── frontend\                 # Vue3 前端工程（前台 + 后台，路由区分）
│   ├── src\
│   ├── index.html
│   └── package.json
├── prototype\                # 高保真 HTML 原型（已完成）
├── blog.sql                  # 数据库建表脚本（已完成）
├── 个人博客系统-详细设计说明书.md
├── 个人博客系统-UI设计规范.md
└── 个人博客系统-技术栈文档.md   # 本文档
```

---

## 9. 版本约束与注意事项

1. **JDK 版本锁定 21**：不要在本项目误用 JDK 8 或 JDK 26，避免 Spring Boot 兼容问题。若命令行 `java -version` 非 21，需检查环境变量 Path 顺序。
2. **Spring Boot 3.x 使用 `jakarta.*` 命名空间**（不是 `javax.*`），与 Spring Boot 2.x 不同，注意导包。
3. **MySQL 连接密码**：仅写在本地 `application.yml`，不提交到 Git（应加入 `.gitignore`）。
4. **不引入 Redis / 消息队列等**：保持初版精简，跑通核心功能后再按需优化。
5. **前端代理**：开发阶段 Vite 配 `proxy` 将 `/api` 转发到后端 `localhost:8080`，规避跨域。
6. **端口**：后端默认 `8080`，前端默认 `5173`；被占用时更换并同步代理配置。

---

## 10. 后续里程碑（技术层面）

| 阶段 | 内容 | 依赖 |
| --- | --- | --- |
| 1 | 导入 `blog.sql` 建库 | MySQL ✅ |
| 2 | 搭建后端骨架（Result/异常/JWT/文章接口） | JDK 21 + Maven ✅ |
| 3 | 生成管理员 BCrypt 密码并回填 | 阶段 2 |
| 4 | 搭建前端骨架（Vite+Vue3+Element Plus+接入设计令牌） | Node ✅ |
| 5 | 前后端联调核心接口 | 阶段 2、4 |
| 6 | 部署（Nginx 反向代理 + 打包上线） | 全部 |

---

*本文档随技术选型变化持续更新。当前版本 V1.0。*
