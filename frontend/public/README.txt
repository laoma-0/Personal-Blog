把首页顶部横幅（Hero）的背景图片放在这个 public 目录下。

约定文件名：hero-bg.jpg （首页横幅背景）

用法说明：
- 放在 public 目录下的文件，Vite 构建时会原样复制到 dist 根目录。
- 在 CSS / 代码里用绝对路径引用，例如： url('/hero-bg.jpg')
- 换图只要用同名文件覆盖，或改代码里的文件名即可。

建议图片规格：
- 尺寸：1920x500 左右（宽屏横幅）
- 格式：jpg（照片类）或 png（带透明/图形类）
- 大小：压缩到 300KB 以内更好，可用 TinyPNG 压一下，加载更快。
